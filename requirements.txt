# WebTracker dependencies (Windows, Python 3.12).
# Install:  pip install -r requirements.txt
# Then:     playwright install chromium
# Build:    pyinstaller --clean --noconfirm WebTracker.spec
#
# Versions are pinned to the set this exe was built & verified with.

playwright==1.60.0
Flask==3.1.3
Werkzeug==3.1.8
pillow==12.2.0

# Build-only (produces WebTracker.exe). Not needed to merely run from source.
pyinstaller==6.21.0
